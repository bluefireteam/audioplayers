name: audioplayers_platform_interface
resolution: workspace
publish_to: none
description: The platform interface for audioplayers, a Flutter plugin to play multiple audio files simultaneously
version: 7.1.2
homepage: https://github.com/Sebastien-VZN/audioplayers
repository: https://github.com/Sebastien-VZN/audioplayers/tree/main/packages/audioplayers_platform_interface

dependencies:
  collection: ^1.17.1
  flutter:
    sdk: flutter
  meta: ^1.7.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

environment:
  sdk: ^3.6.0
  flutter: '>=3.27.0'
