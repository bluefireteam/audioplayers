import 'package:flutter/material.dart';

import '../components/tab_wrapper.dart';

class StreamsTab extends StatelessWidget {
  const StreamsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TabWrapper(
      children: [
        // getPosition, getDuration, streams
      ],
    );
  }
}
